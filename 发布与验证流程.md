# PinDo 发布与验证流程

## 一次性过渡

beta 7.3 首次安装后，在 Dodo → 设置 → 软件更新通道中选择“测试通道”。这是最后一次需要手动安装测试基础版本。

## 日常开发

1. 代码进入 `main` 后，GitHub 自动运行 **Validate PinDo on Windows**。
2. 验证包含锁定依赖安装、回归测试、桌面架构检查、Windows 打包、真实启动、原生便签检查和窗口截图。
3. 任一步失败都不会生成更新版本；诊断报告和截图保存在该 Action 的 Artifacts。
4. 全部通过后，GitHub 自动创建候选预发布版本，并上传测试通道更新文件。
5. 已选择“测试通道”的 PinDo 点击托盘“更新软件”即可安装，无需手动下载。

## 正式发布

测试确认后，运行 **Promote tested PinDo candidate**，输入候选 tag。该流程直接晋升已测试文件，不会重新打包，保证正式版与实测版完全相同。

## 原则

- 红灯禁止发布；绿灯只进入测试通道。
- Windows 实机确认后才晋升稳定通道。
- 出现问题创建新候选版本，不覆盖旧安装包。
