# PinDo 1.1.0-beta.6.1

本版本回退 beta.6.0 中引起显示错误的便签顶部补色和窗口内侧边预览，并保留已经确认正常的四项功能。

- 恢复便签原有顶部结构与圆角，不再额外绘制背景层。
- 侧边虚线框改为独立原生预览窗口，固定在显示器边缘。
- 侧边标签以右边缘为锚点轻微放大，重新显示标题与摘要详情。
- Dodo 原生裁切范围包含完整动画画布，抓起动画不再被按钮边界裁掉。
- 设置只显示设置卡片，不再绘制全屏蒙版或模糊 Dodo。
- 文件夹统一显示清晰文件夹字形，历史文件夹项目自动迁移，不再误显示为系统盘。
- 呼吸与睡眠、失落、沮丧之间增加旧帧淡出、新状态淡入；退出时反向衔接。

退出托盘中的旧版后，在保留 node_modules 的工程中运行 npm start。重点复测本轮六张截图对应场景。
