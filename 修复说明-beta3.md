# PinDo 1.1.0-beta.3 — Dodo 点击范围及 Pin 悬停修复

已定位原因：Dodo 的按钮 width/height:100% 继承了 430×760 透明控制窗口，覆盖大片空白及菜单。原生 Pin 在常态 top:8px 与更高优先级 hover top:-31px 之间切换，造成悬停反复进入/离开。

修改：
- Dodo 按钮固定为 116×146 乘用户 Dodo 缩放系数，不继承控制窗口尺寸。逐帧画布继续保留原有 184×184 的显示尺寸，不改动画资产。
- 菜单明确位于角色按钮上层；大尺寸时限制菜单高度并允许内部滚动。
- 鼠标在透明空白区域时窗口穿透；角色、展开的菜单和提醒框保持可操作；打开设置时保留模态关闭交互。
- 原生拖动期间保留鼠标事件，避免中途穿透；透明区域检测由主进程读取系统鼠标坐标，避免穿透后无法恢复。
- Pin 所有状态使用相同位置和尺寸；Dodo 按钮本身不再发生 hover 平移或缩放，逐帧动画仍播放。
- 沿用 beta.2 已验证的便签原生拖动逻辑。

验证：21 项 Node 测试、JavaScript 语法检查通过，桌面结构检查通过。新增覆盖空白穿透、菜单开关、拖动保持、负坐标显示器和非法区域数据。当前环境无法运行 Windows 实机测试，浏览器运行环境下载超时，未进行视觉自动验收。

测试步骤：退出旧 PinDo，解压到新目录，运行 Windows-一键打包.cmd，安装 release 中 beta.3 安装包。
1. 移到 Dodo 周围空白，应该可以正常点击桌面，不弹助手菜单。
2. 点击 Dodo，菜单内新建/设置等控件应可操作。
3. 鼠标静止悬停 Pin，应不再上下跳动。
4. 拖动 Dodo 并松开，再操作菜单；检查 50%、100%、200% Dodo 大小。

本包是源代码，需要在 Windows 打包，不是预编译 exe。

实现依据：Electron 官方鼠标穿透接口说明 https://www.electronjs.org/docs/latest/tutorial/custom-window-interactions
