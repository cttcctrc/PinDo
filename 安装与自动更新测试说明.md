# PinDo 安装与自动更新测试

本轮先验证 Windows 安装，再验证本地自动更新。测试不依赖 GitHub，也不会产生服务器费用。

## 一、生成并安装 beta 6.8

1. 双击 `Windows-一键打包.cmd`。
2. 打包成功后，打开 `release` 文件夹。
3. 确认文件夹内同时存在 `.exe` 安装程序与 `latest.yml`。
4. 运行安装程序，完成安装并启动 PinDo。
5. 检查 Dodo、便签、托盘、开机启动、退出、重新启动和卸载。

不要删除已经安装的 beta 6.8，它是升级测试的基线。

## 二、生成 beta 6.9 升级测试包

1. 双击 `Windows-生成升级测试版.cmd`。
2. 成功后确认 `release-update` 内存在 beta 6.9 的 `.exe`、`latest.yml` 和 `.blockmap`。
3. 不要手动安装 beta 6.9。

## 三、验证自动更新

1. 双击 `Windows-启动本地更新测试.cmd`，保持窗口开启。
2. 启动已经安装的 beta 6.8。
3. 等待约 8 秒，或右键系统托盘中的 PinDo 图标并选择“更新软件”。
4. PinDo 应检测到 beta 6.9，下载完成后询问是否安装。
5. 安装更新并重新打开 PinDo，确认便签数据仍然存在。

本地测试地址为 `http://127.0.0.1:8787/`，只允许当前电脑访问。正式发布前会替换为 HTTPS 公网更新地址。

## 常见问题

- 提示找不到更新目录：先生成 beta 6.9 升级测试包。
- 检测不到更新：确认本地更新服务器窗口仍然开启，并检查 `release-update/latest.yml`。
- 杀毒软件拦截：先测试 `release/win-unpacked/PinDo.exe`，并保留完整报错信息。
- 打包下载慢：脚本已使用国内镜像；首次下载 Electron 仍可能需要较长时间。
