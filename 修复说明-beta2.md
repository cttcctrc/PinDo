# PinDo 1.1.0-beta.2 拖动修复测试版

本次修复：
- 原生便签移动时不再同时移动窗口内的内容，避免重复位移和裁切。
- 原生窗口移动/缩放采用 Electron 屏幕坐标和固定起点，避免窗口移动引起鼠标局部坐标反复变化。
- 原生便签缩放不再按窗口内部尺寸重算屏幕位置，不重建正在拖动的 DOM。
- Dodo 拖动改为移动透明系统窗口；角色保持固定的窗口内位置。
- 处理指针取消和捕获丢失，避免残留拖动状态。
- 独立便签窗口不再启动隐藏的 Dodo 提醒定时器。

验证：20 项 Node 测试通过，JavaScript 语法与桌面结构检查通过。未在 Windows 实机验证，尤其 Explorer 挂载、不同 DPI、多显示器仍需实测。本包为完整源代码，不是已经生成的 exe。

测试：退出旧 PinDo，解压本包到新目录，运行 Windows-一键打包.cmd。成功后从 release 目录获取安装程序。不要把新旧源码混合覆盖。原有用户数据不主动清除。

重点检查：持续拖动便签、往返拖动 Dodo、缩放便签、拖到屏幕边缘、125%/150% 系统缩放下拖动。确认内容不偏移，松手后窗口不跳回。
